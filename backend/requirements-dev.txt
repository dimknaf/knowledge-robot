# Dev-only deps. NOT installed into the production image (the Dockerfile only
# runs `pip install -r requirements.txt`). Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt
# Then: pytest tests/

pytest==8.3.4
pytest-asyncio==0.25.2
