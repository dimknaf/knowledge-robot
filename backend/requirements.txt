# Versions pinned to what was verified-working in the container.
# To upgrade: bump these, rebuild + retest, then commit the new pins.

# Core web framework
Flask==3.1.3
flask-cors==6.0.2
gunicorn==25.3.0

# OpenAI Agents SDK and MCP (LiteLLM adapter for multi-provider)
openai-agents[litellm]==0.14.8
litellm==1.83.7

# Local scraping path (Playwright for SERP / interactive, crawl4ai for clean markdown)
playwright==1.59.0
crawl4ai==0.8.6

# Data validation and modeling
pydantic==2.12.5
pydantic-settings==2.14.0

# Monitoring and observability
newrelic==12.1.0

# Utilities
python-dotenv==1.0.1
